import { FirebaseDatepickerAdapterDirective } from '../firebase-datepicker-adapter.directive';

describe('FirebaseDatepickerAdapterDirective', () => {
  it('should create an instance', () => {
    const directive = new FirebaseDatepickerAdapterDirective();
    expect(directive).toBeTruthy();
  });
});
