import { WorkedHoursPipe } from './worked-hours.pipe';

describe('WorkedHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkedHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
